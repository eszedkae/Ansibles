# ossec-whitelist-ansible
Ansible playbook for whitelisting IP in ossec

### To Run
    ansible-playbook -i hosts playbook.yml

### Make Changes
-   Change the value of *server_ip* in playbook.yml to something that you want to whitelist.

